<!DOCTYPE html>
<html lang="en">

<head>

    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <meta name="description" content="">
    <meta name="author" content="">

    <title>Hobbits Heladeras Comunitarias</title>

    <!-- Custom fonts for this template -->
    <link href="/SALVACIONDDS/vendor/fontawesome-free/css/all.min.css" rel="stylesheet" type="text/css">
    <link
            href="https://fonts.googleapis.com/css?family=Nunito:200,200i,300,300i,400,400i,600,600i,700,700i,800,800i,900,900i"
            rel="stylesheet">

    <!-- Custom styles for this template -->
    <link href="/SALVACIONDDS/css/sb-admin-2.min.css" rel="stylesheet">

    <!-- Custom styles for this page -->
    <link href="/SALVACIONDDS/vendor/datatables/dataTables.bootstrap4.min.css" rel="stylesheet">

    <style>

        .dropdown-item img {
            width: 20px;
            height: 20px;
            object-fit: contain;
            margin-right: 8px;
        }

    .btn-secondary.dropdown-toggle {
      display: flex;
      align-items: center;
      justify-content: space-between;
      padding: 10px 15px;
      width: 100%;
      max-width: 100%;
      border-radius: 20px;
    }

    .btn-secondary.dropdown-toggle img {
      width: 20px;
      height: 20px;
      object-fit: contain;
      margin-right: 10px;
    }
    </style>
</head>

<body>
<nav class="navbar navbar-expand navbar-light topbar mb-4 static-top shadow" style="background-color: #2a4d69">

    <!-- Logo a la izquierda -->
    <a class="navbar-brand" href="#">
        <img src="/SALVACIONDDS/img/LogoHobbitsB.png" alt="Logo" style="height: 70px; width: auto;">
    </a>

    <!-- Barra de navegación -->
    <ul class="navbar-nav ml-auto">

        <!-- Nav Item - User Information -->
        <li class="nav-item dropdown arrow">
            <a class="nav-link dropdown-toggle" href="#" id="userDropdown" role="button" data-toggle="dropdown"
               aria-haspopup="true" aria-expanded="false">
                <span class="mr-2 d-none d-lg-inline text-white-600 large">Usario</span>
                <img class="img-profile rounded-circle" src="/SALVACIONDDS/img/undraw_profile.svg">
            </a>
            <!-- Dropdown - User Information -->
            <div class="dropdown-menu dropdown-menu-right shadow animated--grow-in" aria-labelledby="userDropdown">
                <!-- Dropdown Menu Items -->
                <a class="dropdown-item" href="#"><i class="fas fa-file-alt fa-sm fa-fw mr-2 text-gray-400"></i> Reporte
                    semanal</a>
                <a class="dropdown-item" href="#"><i class="fas fa-coins fa-sm fa-fw mr-2 text-gray-400"></i> Canje de
                    puntos</a>
                <a class="dropdown-item" href="#"><i class="fas fa-edit fa-sm fa-fw mr-2 text-gray-400"></i> Gestionar
                    heladeras</a>
                <a class="dropdown-item" href="#"><i class="fas fa-lightbulb fa-sm fa-fw mr-2 text-gray-400"></i> Ver
                    contribuciones</a>
                <a class="dropdown-item" href="#"><i class="fas fa-history fa-sm fa-fw mr-2 text-gray-400"></i>
                    Historial de canjes</a>
                <div class="dropdown-divider"></div>
                <a class="dropdown-item" href="#"><i class="fas fa-flag fa-sm fa-fw mr-2 text-gray-400"></i> Reportar
                    falla técnica</a>
                <a class="dropdown-item" href="#"><i class="fas fa-cogs fa-sm fa-fw mr-2 text-gray-400"></i>
                    Configuración</a>
                <a class="dropdown-item" href="#" data-toggle="modal" data-target="#logoutModal"><i
                        class="fas fa-sign-out-alt fa-sm fa-fw mr-2 text-gray-400"></i> Cerrar Sesión</a>
            </div>
        </li>

    </ul>

</nav>
<div>
    <a href="/SALVACIONDDS/inicioPersonaFisica.mustache"
       style="position: absolute; top: 80px; left: 35px; color: #000000; font-size: 24px;">
        <i class="fas fa-arrow-left"></i> Volver
    </a>
</div>


<div class="container mt-5">
    <div class="card o-hidden border-0 shadow-lg my-5">
        <form method="post" action="/distribucionViandas">
            <div class="row justify-content-center">

                <div class="row mb-3 mx-3">
                    <div class="text-center p-3">
                        <h1 class="h4 text-gray-900 mb-2">Distribución de viandas</h1>
                    </div>
                    <div class="col-12">
                        <label for="inputHeladeraOrigen" class="form-label">Seleccione la heladera origen</label>


                        <div class="dropdown">
                            <button class="btn btn-secondary dropdown-toggle w-100 text-start" type="button"
                                    id="menuHeladeraOrigen" data-bs-toggle="dropdown" aria-expanded="false">
                                Seleccione una heladera
                            </button>
                            <input type="hidden" id="selectedHeladeraIdOrigen" name="selectedHeladeraIdOrigen">

                            <ul class="dropdown-menu w-100" id="dropdownHeladerasOrigen" aria-labelledby="menuHeladera">
                                <!-- Las opciones serán cargadas dinámicamente aquí -->
                            </ul>
                        </div>

                    </div>

                    <div class="col-12">
                        <label for="inputHeladeraDestino" class="form-label">Seleccione la heladera destino</label>


                        <div class="dropdown">
                            <button class="btn btn-secondary dropdown-toggle w-100 text-start" type="button"
                                    id="menuHeladeraDestino" data-bs-toggle="dropdown" aria-expanded="false">
                                Seleccione una heladera
                            </button>
                            <input type="hidden" id="selectedHeladeraIdDestino" name="selectedHeladeraIdDestino">

                            <ul class="dropdown-menu w-100" id="dropdownHeladerasDestino"
                                aria-labelledby="menuHeladera">
                                <!-- Las opciones serán cargadas dinámicamente aquí -->
                            </ul>
                        </div>
                    </div>
                        <div class="col-md-6 mb-3">
                            <label for="inputCantidadViandas" class="form-label">Ingrese cantidad de viandas a
                                mover</label>
                            <input type="number" class="form-control shadow-sm" id="inputCantidadViandas"
                                   name="inputCantidadViandas" placeholder="Cantidad">
                        </div>
                        <div class="col-md-6 mb-3">
                            <label for="inputFechaDistribucion" class="form-label">Fecha a realizar distribución</label>
                            <input type="date" class="form-control shadow-sm" id="inputFechaDistribucion"
                                   name="inputFechaDistribucion" placeholder="DD/MM/YYYY">

                        </div>

                    <h5 class="mx-3" style="color:#000000">Seleccione motivo de distribución</h5>
                    <div class="row mb-3 mx-3">
                        <div class="col-md-6">
                            <div class="form-check">
                                <input class="form-check-input" type="radio" value="desperfecto" id="motivoDesperfecto"
                                       name="motivoDistribucion" required>
                                <label class="form-check-label" for="motivoDesperfecto">Desperfecto de la
                                    heladera</label>
                            </div>
                        </div>
                        <div class="col-md-6">
                            <div class="form-check">
                                <input class="form-check-input" type="radio" value="faltantes" id="motivoFaltantes"
                                       name="motivoDistribucion" required>
                                <label class="form-check-label" for="motivoFaltantes">Falta de viandas en heladera
                                    destino</label>
                            </div>
                        </div>
                    </div>

                    <div class="row mb-3 mx-3">
                        <div class="col-12 text-center">
                            <button type="submit" class="btn btn-success btn-custom"
                                    style="background-color: #f69000; border-color: #f69000;">Colaborar ahora
                            </button>
                        </div>
                    </div>
                </div>
        </form>
    </div>
</div>

<!-- Scroll to Top Button-->
<a class="scroll-to-top rounded" href="#page-top">
    <i class="fas fa-angle-up"></i>
</a>

<!-- Logout Modal-->
<div class="modal fade" id="logoutModal" tabindex="-1" role="dialog" aria-labelledby="exampleModalLabel"
     aria-hidden="true">
    <div class="modal-dialog" role="document">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title" id="exampleModalLabel">Seguro que quieres terminar?</h5>
                <button class="close" type="button" data-dismiss="modal" aria-label="Close">
                    <span aria-hidden="true">×</span>
                </button>
            </div>
            <div class="modal-body">Selecciona "Salir" para cerrar sesión</div>
            <div class="modal-footer">
                <button class="btn btn-secondary" type="button" data-dismiss="modal">Cancelar</button>
                <a class="btn btn-danger" href="/login">Salir</a>
            </div>
        </div>
    </div>

</div>

<!--
<script>


    // URL de la API
    const apiUrl = '/api/heladerasDTO';

    // Función para cargar las heladeras en un desplegable específico
    async function cargarHeladeras(menuId, dropdownId, inputId) {
        try {
            console.log(`Cargando heladeras para: ${menuId}, ${dropdownId}, ${inputId}`);

            const menuHeladera = document.getElementById(menuId);
            const dropdownHeladeras = document.getElementById(dropdownId);
            const inputHidden = document.getElementById(inputId);

            if (!menuHeladera || !dropdownHeladeras || !inputHidden) {
                throw new Error(`Elementos no encontrados: menuId=${menuId}, dropdownId=${dropdownId}, inputId=${inputId}`);
            }

            const response = await fetch(apiUrl);
            if (!response.ok) {
                throw new Error(`Error al obtener las heladeras: ${response.statusText}`);
            }

            const heladeras = await response.json();
            console.log('Heladeras obtenidas:', heladeras);

            heladeras.forEach(heladera => {
                const li = document.createElement('li');
                li.innerHTML = `
                    <a class="dropdown-item d-flex align-items-center" href="#" data-id="${heladera.idHeladera}">
                        <img src="https://cdn-icons-png.flaticon.com/512/4151/4151152.png" alt="Ícono" class="me-2">
                        <div>
                            <strong>${heladera.nombre}</strong><br>
                            <small>ID: ${heladera.idHeladera}</small>
                        </div>
                    </a>
                `;

                li.querySelector('a').addEventListener('click', function (event) {
                    event.preventDefault();
                    menuHeladera.innerHTML = `
                        <img src="https://via.placeholder.com/20" alt="Ícono" class="me-2">
                        <strong>${heladera.nombre}</strong><br>
                        <small>ID: ${heladera.idHeladera}</small>
                    `;
                    inputHidden.value = heladera.idHeladera;
                });

                dropdownHeladeras.appendChild(li);
            });
        } catch (error) {
            console.error('Error al cargar las heladeras:', error);
        }
    }

    // Ejecutar las funciones al cargar el DOM
    document.addEventListener('DOMContentLoaded', () => {
        cargarHeladeras('menuHeladeraOrigen', 'dropdownHeladerasOrigen', 'selectedHeladeraIdOrigen');
        cargarHeladeras('menuHeladeraDestino', 'dropdownHeladerasDestino', 'selectedHeladeraIdDestino');
    });
</script>-->
<script>const apiUrl = '/api/heladeras';

// Referencias a los elementos
const menuHeladeraOrigen = document.getElementById('menuHeladeraOrigen');
const dropdownHeladerasOrigen = document.getElementById('dropdownHeladerasOrigen');
const menuHeladeraDestino = document.getElementById('menuHeladeraDestino');
const dropdownHeladerasDestino = document.getElementById('dropdownHeladerasDestino');
const inputCantidadViandas = document.getElementById('inputCantidadViandas');
const botonCargar = document.querySelector('button[type="submit"]');
const mensajeError = document.createElement('div');

// Variables para mantener las selecciones actuales y capacidades
let heladeraOrigenSeleccionada = null;
let heladeraDestinoSeleccionada = null;
let heladeras = [];

// Función para cargar las heladeras en los desplegables
async function cargarHeladeras() {
    try {
        const response = await fetch(apiUrl);
        if (!response.ok) throw new Error('Error al obtener las heladeras');

        heladeras = await response.json();
        cargarHeladerasEnDropdown(menuHeladeraOrigen, dropdownHeladerasOrigen, 'selectedHeladeraIdOrigen', heladeras, 'origen');
        cargarHeladerasEnDropdown(menuHeladeraDestino, dropdownHeladerasDestino, 'selectedHeladeraIdDestino', heladeras, 'destino');
    } catch (error) {
        console.error('Error al cargar las heladeras:', error);
    }
}

// Función para cargar opciones en un desplegable
function cargarHeladerasEnDropdown(menu, dropdown, inputHiddenId, heladeras, tipo) {
    dropdown.innerHTML = '';
    heladeras.forEach(heladera => {
        const li = document.createElement('li');
        li.innerHTML = `
            <a class="dropdown-item d-flex align-items-center ${isOptionDisabled(heladera.idHeladera, tipo) ? 'disabled' : ''}" href="#" data-id="${heladera.idHeladera}">
                <img style="height:20px ; width:auto;" src="https://cdn-icons-png.flaticon.com/512/4151/4151152.png" alt="Ícono" class="me-2">
                <div>
                    <strong>${heladera.nombre}</strong><br>
                    <small>ID: ${heladera.idHeladera}</small>
                </div>
            </a>
        `;

        const link = li.querySelector('a');
        if (!link.classList.contains('disabled')) {
            link.addEventListener('click', function (event) {
                event.preventDefault();
                menu.innerHTML = `
                    <img src="https://cdn-icons-png.flaticon.com/512/4151/4151152.png" alt="Ícono" class="me-2">
                    <strong>${heladera.nombre}</strong><br>
                    <small>ID: ${heladera.idHeladera}</small>
                `;
                document.getElementById(inputHiddenId).value = heladera.idHeladera;
                if (tipo === 'origen') heladeraOrigenSeleccionada = heladera.idHeladera;
                else heladeraDestinoSeleccionada = heladera.idHeladera;
                cargarHeladeras();
                validarCapacidad();
            });
        }
        dropdown.appendChild(li);
    });
}

// Función para verificar si una opción debe estar deshabilitada
function isOptionDisabled(heladeraId, tipo) {
    return (tipo === 'origen' && heladeraId === heladeraDestinoSeleccionada) ||
        (tipo === 'destino' && heladeraId === heladeraOrigenSeleccionada);
}

// Validar capacidad máxima de la heladera destino
function validarCapacidad() {
    const heladeraDestino = heladeras.find(h => h.idHeladera === heladeraDestinoSeleccionada);
    const cantidadViandas = parseInt(inputCantidadViandas.value, 10);
    mensajeError.className = 'text-danger mt-2';
    mensajeError.innerHTML = '';

    if (heladeraDestino && cantidadViandas > heladeraDestino.capacidad_maxima) {
        mensajeError.textContent = `Error: La cantidad de viandas (${cantidadViandas}) excede la capacidad máxima (${heladeraDestino.capacidad_maxima}).`;
        if (!inputCantidadViandas.parentNode.contains(mensajeError)) {
            inputCantidadViandas.parentNode.appendChild(mensajeError);
        }
        inputCantidadViandas.classList.add('is-invalid');
        botonCargar.disabled = true;
    } else {
        inputCantidadViandas.classList.remove('is-invalid');
        if (inputCantidadViandas.parentNode.contains(mensajeError)) {
            inputCantidadViandas.parentNode.removeChild(mensajeError);
        }
        botonCargar.disabled = false;
    }
}

// Evento para validar al cambiar la cantidad de viandas
inputCantidadViandas.addEventListener('input', validarCapacidad);

// Cargar las heladeras al cargar la página
document.addEventListener('DOMContentLoaded', cargarHeladeras);
</script>

<!-- Bootstrap core JavaScript-->
<script src="/SALVACIONDDS/vendor/jquery/jquery.min.js"></script>
<script src="/SALVACIONDDS/vendor/bootstrap/js/bootstrap.bundle.min.js"></script>

<!-- Core plugin JavaScript-->
<script src="/SALVACIONDDS/vendor/jquery-easing/jquery.easing.min.js"></script>

<!-- Custom scripts for all pages-->
<script src="/SALVACIONDDS/js/sb-admin-2.min.js"></script>

<!-- Page level plugins -->
<script src="/SALVACIONDDS/vendor/datatables/jquery.dataTables.min.js"></script>
<script src="/SALVACIONDDS/vendor/datatables/dataTables.bootstrap4.min.js"></script>

<!-- Page level custom scripts -->
<script src="/SALVACIONDDS/js/demo/datatables-demo.js"></script>
<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>

</body>

</html>